# Python
__pycache__/
*.py[cod]
.venv/
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/

# Environment. .env.example is the template and is committed.
.env
.env.*
!.env.example
# Streamlit reads secrets from here; config.toml beside it is committed.
.streamlit/secrets.toml

# Generated data. The scraped corpus is deliberately not ignored: it is
# committed so a reviewer never has to re-crawl the hospital's website.
knowledge/hospital.db
knowledge/conversations.db
knowledge/qdrant/
